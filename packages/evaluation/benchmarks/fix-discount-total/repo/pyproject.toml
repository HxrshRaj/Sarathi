[project]
name = "shopfront-demo"
version = "0.1.0"
description = "Intentionally small checkout app used by CodePilot benchmarks."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-q"
